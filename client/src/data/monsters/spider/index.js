import spiderSprite from './spider.png';
//spiders would damage you for 1 if touched by adjacent square, otherwise
// eslint-disable-next-line to the line before.
const Spider = {
  hp: 12,
  maxHp: 12,
  damage: 1,
  defence: 2,
  exp: 400,
  type: 'spider',
  sprite: spiderSprite
};

export default Spider;